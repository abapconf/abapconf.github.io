<mvc:View
	controllerName="abapconf.2024.org.controller.App"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">
    <Shell xmlns="sap.m" appWidthLimited="false">
        <App
            id="app"
            busy="{appView>/busy}"
            busyIndicatorDelay="{appView>/delay}"/>
    </Shell>
</mvc:View>
